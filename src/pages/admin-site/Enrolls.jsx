import React from 'react'

const Enrolls = () => {
  // const { status, error, response, fetchData } = useFetch()

  // if (status === 'success') {
  //   setAllCourses(response.allCourses)
  // }
  // if (status === 'error') {
  //   console.log('courses fetch error', error)
  // }
  return (
    <div>Enrolls</div>
  )
}

export default Enrolls